# Release V8.2 Linux

Base: V8 auditada + correção Prisma 7 + correção CSS da V8.1.

Adições desta release:

- `.nvmrc` fixando Node 22 como recomendação;
- `engines` no package raiz;
- `scripts/setup-linux.sh`;
- `scripts/dev-linux.sh`;
- `scripts/reset-linux.sh`;
- scripts npm `linux:setup`, `linux:dev`, `linux:reset`;
- documentação `LINUX-START.md`;
- preservação integral dos módulos funcionais da V8.1.

Nenhuma funcionalidade foi substituída pela Demo Local.
